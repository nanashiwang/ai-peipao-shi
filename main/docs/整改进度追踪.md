# AI 陪跑师效率系统 · 整改进度追踪

> 基线文档：`docs/生产就绪度评估报告.md`
> 当前批次：批次 5 · Alembic 迁移 baseline（代码完成 / 待提交 / 待部署）
> 状态更新时间：2026-07-03

## 当前进度

| P0 | 阻断项 | 当前状态 | 说明 |
|---|---|---|---|
| P0-1 | 默认配置鉴权关闭 / 弱口令绕过校验 | 已提交、已部署并验证 | 部署环境 `pilot/staging/docker/production/prod` 禁止 `ADMIN_AUTH_REQUIRED=false`，弱 `ADMIN_AUTH_SECRET`、SQLite、弱 PostgreSQL 口令均拒绝启动。线上 `ADMIN_AUTH_REQUIRED=true`，`/api/admin/auth/status` 返回 `auth_required=true`。 |
| P0-2 | 公开端点签发管理员 token | 已提交、已部署并验证 | `/api/test-chat/login` 不再是公开路径；部署环境中即使带鉴权也禁用测试登录口。线上匿名请求返回 `401`，不再公开发放管理员 token。 |
| P0-3 | `/api/rpa/*` 匿名注入/枚举 | 已提交、已部署并验证 | `/api/rpa/conversations/sync` 与 `resolve` 强制校验设备头，并校验目标会话属于该设备范围。线上有效请求体匿名访问 `sync`/`resolve` 均返回 `401 缺少设备令牌`。 |
| P0-4 | 真实密钥明文落盘 | 代码已部署，待人工轮换泄露密钥 | Ark 客户端和运行校验支持 `ARK_API_KEY`/`ARK_ENDPOINT_ID` 环境变量优先，文件仅作本地兜底。已泄露 DashScope key、`admin_secret`、`device_token` 仍需人工在对应平台/设备侧轮换。 |
| P0-5 | send-artifacts 截图 IDOR | 已提交、已部署并验证 | 截图文件名改为随机不可枚举；访问端点要求管理员 Bearer，并按 `SendLog.family_id` 做家庭范围校验；前端改为带授权头读取截图。 |
| P0-6 | 无迁移工具（裸 create_all） | 代码完成、本地验证通过，待提交部署 | 引入 Alembic baseline：部署环境 `pilot/staging/docker/production/prod` 默认执行 `alembic upgrade head`，本地/测试默认保留 `create_all + ensure_columns`；baseline 只创建缺失表、补历史列并记录 `alembic_version`，不 drop、不重建生产表。Docker 镜像已复制 `alembic.ini` 与 `migrations/`。 |
| P0-7 | PostgreSQL 生产备份失效 | 已提交、已部署并验证 | 备份服务从 SQLite-only 扩展为按 `DATABASE_URL` 自动分流：SQLite 继续用在线 backup API，PostgreSQL 使用 `pg_dump --format=plain --no-owner --no-privileges` 生成 `.postgres.sql`；API 镜像安装 `postgresql-client`；`./data:/app/data` 挂载保证备份落宿主机持久目录；恢复演练对 SQLite 做完整性检查，对 PostgreSQL dump 做核心表结构校验。 |
| P0-8 | 敏感截图与 RPA result/debug 截图无限留存 | 已提交、已部署并验证 | RPA result 截图默认仅用于上传 payload，随后立即删除本地文件；真实发送模式禁止开启 `keep_debug_screenshots`/`keep_result_screenshots`；RPA 启动时按 `local_screenshot_retention_days` 清理本机遗留截图和临时 JSON；服务端 retention 覆盖 `rpa/debug_*.png`、`rpa/result_*.png`、`.tmp_rpa_*.json`。历史本地敏感截图的物理删除仍需单独确认。 |
| P0-12 | 前端 XSS 无兜底 + token 存 localStorage | 已提交、已部署并验证 | 新增 `/static/security.js` 在 `app.js` 前加载：将 `controlUser`/`chatUser` 从 `localStorage` 迁移到 `sessionStorage` 并拦截后续写入；为 `innerHTML`、`insertAdjacentHTML`、`setAttribute` 增加基础 HTML/URL 协议清洗，阻断 `javascript:`/`data:` 等危险协议和非白名单内联事件；首页加入兼容现有内联事件的 CSP、`nosniff` 和 `Referrer-Policy`，并将 `security.js` 纳入静态资源 hash。 |
| 部署脚本 | fail-closed 配套 | 已提交、已部署并验证 | `deploy/docker-deploy.sh` 会在缺少 `.env` 密钥时自动生成强随机 `ADMIN_AUTH_SECRET` 与 `POSTGRES_PASSWORD`。本次部署遇到历史 PostgreSQL volume 仍使用旧口令，已在服务器用当前 `.env` 强口令同步轮换 `coach` 用户密码，未删除数据卷。 |

## 本批验证

- `python -m pytest tests/test_admin_auth.py tests/test_runtime_config.py tests/test_ark_client_config.py tests/test_send_task_validation.py -q`
  - 结果：`102 passed`
- `python -m pytest tests/test_retention_service.py -q`
  - 结果：`2 passed`
- `python -m pytest -q`
  - 结果：`254 passed, 8 skipped, 11 failed`
  - 说明：11 个失败仍集中在已知待分诊区域：7 个发送重试、3 个侧边栏导航、1 个周报发送闭环；本批安全相关定向测试已通过。
- `python -m compileall app rpa`
  - 结果：通过
- `docker compose config --quiet`
  - 结果：本机缺少 Docker，未执行；服务器侧已通过 `docker compose up -d --build --force-recreate api` 完成重建。
- 服务器部署验证
  - 提交：`ec2f742 安全整改：收紧密钥与设备鉴权`
  - 容器：`postgres` healthy，`api` running
  - 健康检查：`/health` 返回 `{"ok":true,"mode":"pilot","database":"postgresql","config_status":"ok"}`
  - 关键鉴权：`/api/test-chat/login` 匿名返回 `401`；`/api/rpa/conversations/sync` 和 `resolve` 匿名返回 `401`
- 批次 2 本地验证
  - `python -m pytest main/tests/test_retention_service.py -q`
    - 结果：`2 passed`
  - `python -m pytest main/tests/test_wecom_sender_post_send_sync.py -q`
    - 结果：`11 skipped`（本机缺少 Windows/RPA 依赖，沿用既有跳过策略）
  - `python -m pytest main/tests/test_ops_health.py -q`
    - 结果：`5 passed`
  - `python -m compileall main/app main/rpa`
    - 结果：通过
- 批次 2 服务器部署验证
  - 提交：`e03ba03 整改：治理RPA截图留存`
  - 容器：`postgres` healthy，`api` running
  - 健康检查：`/health` 返回 `{"ok":true,"mode":"pilot","database":"postgresql","config_status":"ok"}`
  - retention 冒烟：API 容器内可导入 `rpa_artifact_retention_plan`，默认 `rpa_artifact_days=7`
- 批次 3 本地验证
  - `python -m pytest main/tests/test_backup_service.py -q`
    - 结果：`6 passed`
  - `python -m pytest main/tests/test_ops_health.py -q`
    - 结果：`5 passed`
  - `python -m compileall main/app`
    - 结果：通过
- 批次 3 服务器部署验证
  - 提交：`727077b fix: 持久化生产备份目录`
  - 容器：`postgres` healthy，`api` running
  - 健康检查：`/health` 返回 `{"ok":true,"mode":"pilot","database":"postgresql","config_status":"ok"}`
  - 工具：API 容器 `pg_dump (PostgreSQL) 17.10`
  - 备份冒烟：已生成 `coach_mvp_20260702_184828.postgres.sql`（399355 bytes），`run_restore_drill` 返回 `drill_passed=True`、`missing_tables=[]`
- 批次 4 本地验证
  - `python -m pytest main/tests/test_frontend_security.py -q`
    - 结果：`5 passed`
  - `node --check main/app/static/security.js`
    - 结果：通过
  - `python -m pytest main/tests/test_admin_auth.py main/tests/test_send_task_validation.py -q`
    - 结果：`95 passed`
  - `python -m compileall main/app`
    - 结果：通过
- 批次 4 服务器部署验证
  - 提交：`cbb21d9 整改：增加前端XSS与token兜底`
  - 容器：`postgres` healthy，`api` running
  - 健康检查：`/health` 返回 `{"ok":true,"mode":"pilot","database":"postgresql","config_status":"ok"}`
  - 首页响应头：`Content-Security-Policy`、`X-Content-Type-Options: nosniff`、`Referrer-Policy: same-origin`
  - 首页 HTML：已加载 `/static/security.js?v=46c1ad46`
  - 静态脚本：`/static/security.js` 可访问，包含 `AUTH_STORAGE_KEYS`、`Storage.prototype.setItem`、`sanitizeHTML`
- 批次 5 本地验证
  - `python -m pytest main/tests/test_db_migrations.py -q`
    - 结果：`5 passed`
  - `python -m pytest main/tests/test_db_schema_compat.py main/tests/test_db_migrations.py main/tests/test_backup_service.py main/tests/test_ops_health.py -q`
    - 结果：`19 passed`
  - `python -m compileall main/app main/migrations`
    - 结果：通过

## 尚未完成的人工作业

以下操作仍需单独确认或由平台侧执行：

- 在阿里云控制台吊销并重签已泄露 DashScope key。
- 轮换历史 `admin_secret`、`device_token` 并同步到相关设备/部署环境。
- 确认后删除本地/仓库工作区中的历史敏感截图、临时 JSON、明文配置文件；代码已提供 retention/prune 兜底，但物理清除仍属于危险操作。

## 下一批建议

下一步建议继续处理：

- P0-9：TLS 终止与 8000 本地化
- P0-10：CI/CD + 监控告警 + 日志基础设施
