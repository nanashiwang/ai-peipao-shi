# AI 陪跑师效率系统 · 整改进度追踪

> 基线文档：`docs/生产就绪度评估报告.md`
> 当前批次：批次 1 · 安全与密钥止血
> 状态更新时间：2026-07-03

## 当前进度

| P0 | 阻断项 | 当前状态 | 说明 |
|---|---|---|---|
| P0-1 | 默认配置鉴权关闭 / 弱口令绕过校验 | 代码完成，待提交部署 | 部署环境 `pilot/staging/docker/production/prod` 禁止 `ADMIN_AUTH_REQUIRED=false`，弱 `ADMIN_AUTH_SECRET`、SQLite、弱 PostgreSQL 口令均拒绝启动。 |
| P0-2 | 公开端点签发管理员 token | 代码完成，待提交部署 | `/api/test-chat/login` 不再是公开路径；部署环境中即使带鉴权也禁用测试登录口。 |
| P0-3 | `/api/rpa/*` 匿名注入/枚举 | 代码完成，待提交部署 | `/api/rpa/conversations/sync` 与 `resolve` 强制校验设备头，并校验目标会话属于该设备范围。 |
| P0-4 | 真实密钥明文落盘 | 代码侧完成，待人工轮换 | Ark 客户端和运行校验支持 `ARK_API_KEY`/`ARK_ENDPOINT_ID` 环境变量优先，文件仅作本地兜底。已泄露 DashScope key、`admin_secret`、`device_token` 仍需人工轮换。 |
| P0-5 | send-artifacts 截图 IDOR | 代码完成，待提交部署 | 截图文件名改为随机不可枚举；访问端点要求管理员 Bearer，并按 `SendLog.family_id` 做家庭范围校验；前端改为带授权头读取截图。 |
| 部署脚本 | fail-closed 配套 | 代码完成，待服务器验证 | `deploy/docker-deploy.sh` 会在缺少 `.env` 密钥时自动生成强随机 `ADMIN_AUTH_SECRET` 与 `POSTGRES_PASSWORD`；若检测到历史弱数据库口令，会尝试同步轮换 PostgreSQL 用户密码，避免新安全校验导致一键部署直接失败。 |

## 本批验证

- `python -m pytest tests/test_admin_auth.py tests/test_runtime_config.py tests/test_ark_client_config.py tests/test_send_task_validation.py -q`
  - 结果：`102 passed`
- `python -m pytest tests/test_retention_service.py -q`
  - 结果：`2 passed`
- `python -m pytest -q`
  - 结果：`254 passed, 8 skipped, 11 failed`
  - 说明：11 个失败仍集中在已知待分诊区域：7 个发送重试、3 个侧边栏导航、1 个周报发送闭环；本批安全相关定向测试已通过。
- `python -m compileall app rpa`
  - 结果：通过
- `docker compose config --quiet`
  - 结果：本机缺少 Docker，未执行；待服务器部署时用脚本健康检查验证。

## 尚未执行的危险操作

以下操作需要单独确认后执行：

- `git commit`
- `git push`
- 服务器部署
- 删除本地/仓库工作区中的敏感截图、临时 JSON、明文配置文件

## 下一批建议

批次 2 建议继续处理：

- P0-12：前端 XSS 系统性兜底与 token 存储风险
- P0-8：敏感截图与 RPA result/debug 截图留存策略
- P0-7：PostgreSQL 生产备份与恢复演练
